import { PointerDownStopPropagationDirective } from './pointer-down-stop-propagation.directive';

describe('PointerDownStopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerDownStopPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
